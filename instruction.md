There is an access log at `/app/access.log`. Analyze the traffic and write a JSON
report to `/app/report.json` with these fields:

1. `total_requests`: the number of non-empty log lines
2. `unique_ips`: the number of distinct client IP addresses
3. `top_path`: the most frequently requested path

The report must be valid JSON.

You have 120 seconds to complete this task. Do not cheat by using online solutions or hints specific to this task.
